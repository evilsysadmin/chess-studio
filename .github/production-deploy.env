# Versioned production hosting switch.
# Keep this separate from DEPLOY_ENV: both targets serve production config/data.
# P0 temporary OCI cutover retry after runtime sync heredoc fix.
DEPLOY_TARGET=oci
