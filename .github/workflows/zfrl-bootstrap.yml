name: Apply zfrl enemy doctrine
on:
  push:
    branches: [feat/zfrl-enemy-doctrine]
permissions:
  contents: write
jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - name: Apply product delta
        shell: bash
        run: |
          set -euo pipefail
          git apply --check .github/zfrl-bootstrap.patch
          git apply --whitespace=nowarn .github/zfrl-bootstrap.patch
          make static-preflight
          rm -f .github/zfrl-bootstrap.patch .github/workflows/zfrl-bootstrap.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: add Intel-backed enemy doctrines"
          git push origin HEAD:feat/zfrl-enemy-doctrine
