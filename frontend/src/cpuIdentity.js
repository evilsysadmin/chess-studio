import { MATTHIAS_BASE_AVATAR } from './matthiasVisuals.js';
import matthiasBrandMark from './assets/matthias-brand.png';

export const CPU_IDENTITY = Object.freeze({
  name: 'Matthias',
  role: 'CPU',
  avatar: MATTHIAS_BASE_AVATAR,
  brandMark: matthiasBrandMark,
});
