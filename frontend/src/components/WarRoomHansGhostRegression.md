Hans War Room regression contract:

- A new game must receive a fresh 3D scene so prior per-game Hans DOM/runtime state cannot leak.
- Return routes (`service-return`, `mop-return`, `chore-return`) must visibly open the service door near the threshold before Hans disappears from the room.
- The shared Hans service dialogue renderer must surface both service and chore dialogue specs while remaining suppressed in Zen/Focus UI states.
