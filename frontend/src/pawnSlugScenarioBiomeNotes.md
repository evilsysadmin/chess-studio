# Pawn Slug premium biome contract

Premium scenarios are data-driven. Tile maps own visual layout, bounds, and typed markers. `pawnSlugScenarioRenderer.js` interprets scenario tiles into Three.js geometry. Gameplay integration should consume typed markers gradually without duplicating coordinates in renderer code.
