# Ensure that the new dependencies (pydantic, fastapi, pytest) are listed.
# If the project already includes them, this addition is harmless.
[tool.poetry.dependencies]
fastapi = "^0.110.0"
pydantic = "^2.6.0"

[tool.poetry.dev-dependencies]
pytest = "^8.0.0"
pytest-asyncio = "^0.23.0"
