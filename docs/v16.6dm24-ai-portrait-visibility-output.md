# v16.6dm24 · Retrato AI visible y salida completa

## UX

La regeneración manual deja de vivir dentro del disclosure `Ver en qué se basa`. El CTA `↻ Analizarme de nuevo` se muestra directamente debajo del retrato junto con el cooldown. El disclosure conserva únicamente la explicación factual.

## Salida del modelo

El límite de 420 caracteres sigue protegiendo comentarios cortos de partida. `player_portrait` usa un presupuesto propio de 240 tokens y 900 caracteres en Worker, backend, frontend y caché. Los recortes defensivos intentan cerrar en una frase completa para no mostrar palabras amputadas.

El esquema local del retrato sube a v2 para descartar lecturas antiguas ya truncadas y forzar una lectura automática limpia tras desplegar. No cambia el dossier factual, el cooldown server-side de 6 horas para peticiones manuales, la regeneración automática cada tres partidas ni la política de fallback.
