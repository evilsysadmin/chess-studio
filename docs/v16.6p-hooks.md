# v16.6p — self-healing pre-push hook

- `make tests` ya no falla si `.githooks/pre-push` falta por una copia que omitió dotfiles.
- El Makefile crea `.githooks/`, regenera el hook, aplica `chmod +x` y configura `core.hooksPath`.
- El hook versionado sigue incluido en el ZIP.
