# v16.6dm26 · Etiquetas de latencia más explícitas

Cambio exclusivamente de UX en Admin → Observabilidad.

- API / Render: `Latencias p50 / p95 / p99`.
- Workers AI: `Latencias p50 / p95 / p99`.
- Los valores, unidades (`ms`), ventanas, agregación y backend permanecen sin cambios.
