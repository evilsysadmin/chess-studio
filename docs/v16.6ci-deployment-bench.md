# V16.6ci — Banquillo visible

La mesa de guerra convierte el estado de despliegue en una navegación primaria: `Todos`, `Tablero` y `Banquillo`, con contadores reales derivados de `deploymentSummary`. El estado interno sigue llamándose `reserve`; `Banquillo` es sólo la etiqueta operativa de UI.

Los filtros nativos de tipo/orden usan `color-scheme: dark` y opciones con fondo/texto explícitos para mantener contraste al abrir el desplegable.
