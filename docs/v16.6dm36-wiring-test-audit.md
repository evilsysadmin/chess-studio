# v16.6dm36 · Auditoría de wiring/contract tests

## Resultado

Se revisaron todos los tests frontend que leen código fuente directamente. Tras migrar el wiring AI a comportamiento quedan **14 source-reader tests** y **67 assertions candidatas a estar acopladas a implementación**.

El gate `make static-contract-risk-audit` ahora es bloqueante con esos valores como techo: una nueva release no puede aumentar la deuda sin migrar antes otro contrato.

## Migrado en dm36

- `aiNarrativeFeaturesWiring.test.js`: ya no usa `readFileSync`, `toContain()` sobre prompts ni declaraciones exactas de constantes. Importa `modelFor`, los modelos y los builders de dossiers y valida resultados.
- El routing Qwen tiene una única fuente de verdad en el Worker y se prueba por igualdad/comportamiento.
- El contrato de despliegue CI -> Workers AI -> Pages se valida en el preflight operativo, no mediante asserts de copy JSX.

## Source-reader tests restantes

- `adminMobileLayout.test.js`: contrato CSS/responsive.
- `adminUxContract.test.js`: estructura UX Admin; candidato a migrar gradualmente a tests de render/interacción.
- `armyRosterView.test.js`: wiring visual de roster; candidato.
- `campaignOperationalFlow.test.js`: flujo entre vistas Combat; candidato.
- `chessGlossary.test.js`: cobertura de integración de ayudas/glosario.
- `combatBattleLayout.test.js`: estructura crítica de mesa Combat; conservar parcialmente, migrar wiring funcional.
- `combatOperationalUx.test.js`: **hotspot principal (21)**; siguiente objetivo de reducción.
- `foregroundPresenceContract.test.js`: privacidad/cadencia; parte ya cubierta por helpers puros.
- `friendlyUxContract.test.js`: **hotspot UX (12)**; migrar orden/visibilidad a tests de render cuando sea rentable.
- `mechanicTutorials.test.js`: cobertura de tutoriales por modo.
- `narrativeWiring.test.js`: conserva un contrato intencional de orden “backend confirma -> narrativa remota”; candidato a extraer a orquestador testeable.
- `releaseContinuityContract.test.js`: wiring de arranque/recovery; fuente deliberada pero debe seguir reduciéndose.
- `safeStorageUsageContract.test.js`: gate arquitectónico global; lectura de source intencional.
- `zenMode.test.js`: wiring de supresión visual/audio; candidato.

## Regla

Un test de source sólo se acepta si protege un **wiring/arquitectura que no tiene una superficie observable barata**. Copy, prompts, nombres de variables, imports concretos y expresiones JSX no se consideran contratos de producto.
