# v16.6cs · Validación

Cambios:
- CombatBattleView adopta `board-live-row`, `game-music-rail`, `game-board-stack`
  y `game-side-column`, igual que GameScreen.
- El lateral de Combat es `Estado táctico`, sin Game Chat, CpuPresence ni
  reacciones de público.
- Fuerza/Velocidad y la leyenda de nivel pasan a `Ayuda rápida`, plegada.
- El reproductor global se oculta sólo mientras una batalla Combat está activa;
  la mesa usa el mismo rail musical que las partidas normales/torneo.
- Setup, campaña y despliegue mantienen el dock global.
- `CONFIRMAR FUERZA` pasa a `CONFIRMAR DESPLIEGUE`.

Validación ejecutada:
- `make static-preflight`: PASS
- `make test-suite-audit-ci`: PASS
- release consistency: PASS (`v16.6cs`)
- API surface/auth/security gate: PASS
- audio/data UX/campaign map: PASS
- contrato estático específico de la nueva mesa Combat: PASS
- suite detectada: frontend 570 tests / 79 files; backend 162 / 8; E2E 2 / 1

Limitación:
- El ZIP no incluye `node_modules`; Vitest/build Vite completos quedan como gate
  final de GitHub Actions.
