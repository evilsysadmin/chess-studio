# V16.6ch — validación de la iteración

Validado en el entorno de construcción sin acceso de red:

- `make static-preflight` — PASS.
  - catálogo musical: 70 temas, 11 estilos, 69 huellas estructuradas únicas;
  - temas experimentales curados fuera del catálogo visible;
  - data UX, mapa de campaña, release y superficie API;
  - sintaxis Python y JS cubierta por los gates offline.
- `make test-suite-audit-ci` — PASS.
  - 559 tests frontend / 77 ficheros detectados;
  - 162 tests backend / 8 ficheros;
  - 2 E2E / 1 fichero;
  - manifest crítico y wiring de GitHub Actions coherentes.
- `node --check` — PASS para `sound.js`, `combatDeployment.js`, `combatRoster.js` y el gate musical.

No se pudo ejecutar Vitest completo en este runtime porque el `node_modules` incluido en el material de trabajo está incompleto (falta el binario/paquete `vitest` y el entrypoint de `chess.js`) y el entorno no dispone de red para reconstruirlo. GitHub Actions sigue siendo la autoridad final para la suite completa.

Cobertura añadida en la suite del repo:

- `Enviar a reserva` conserva un hueco explícito y no recoloca automáticamente la unidad.
- Dos bajas pendientes se reflejan como `fallenCount=2`; al reemplazarlas reaparecen como reclutas en reserva.
- contrato estático de ghost compacto y casilla de drop resaltada.
- catálogo musical esperado: 70 temas; los seis experimentales curados no pueden reaparecer en selector ni desde una sesión antigua.
