# v16.6bj — release reconciliation

This release deliberately reconciles v16.6bh and v16.6bi into one strict lineage.

- Baseline: v16.6bi (keeps the 20s budget and optimized legal-position fuzz test).
- Re-applies the known-good v16.6bh roster UI explicitly: 16 identities, 6+6+4 desktop grid, centered last row, 4-column tablet and 2-column mobile layouts, full-card dossier interaction, and global campaign-rank clarification.
- Adds `APP_RELEASE = v16.6bj` and shows it next to the build SHA in Admin so a stale Pages deployment can be identified immediately.
- Adds `releaseContinuity.test.js` to both the local Makefile critical gate and GitHub CI. It protects the cumulative roster layout, global-vs-unit rank distinction, campaign profile keys, Zen/glossary continuity, and the CI fuzz budget.

No dependency versions or persistence schemas were changed.
