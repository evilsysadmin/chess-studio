# v16.6ac — bootstrap reparable + catálogo musical abierto

- Una `.venv` existente pero sin `pip` se detecta como corrupta y se recrea automáticamente.
- `backend-install` verifica `pip`, usa `ensurepip` como fallback y actualiza pip antes de instalar requirements.
- Vite separa `three`, React y `chess.js` en chunks explícitos; Board3D sigue lazy.
- `Gambito de marfil` pasa a llamarse `Gambito del rey`.
- Se añaden seis estilos no mediterráneos: Vals del zugzwang, Blues del alfil, Biblioteca bajo nieve, Búnker analógico, Réquiem para una dama y Mercancías 04:12.
- Cada estilo nuevo tiene perfil de arreglo propio: vals/chamber, blues 12/8, minimal sin batería, secuenciador analógico, réquiem órgano/coro y motorik ferroviario.
