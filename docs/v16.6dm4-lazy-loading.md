# v16.6dm4 · Lazy loading

- Las pantallas de juego/modos/análisis/admin/replay se cargan con `React.lazy`.
- El reproductor global se separa del shell mediante Suspense.
- `sound.js` deja de entrar en el bundle de login: se importa sólo con usuario autenticado y perfil listo.
- El masthead permanece visible mientras llega un chunk de ruta.
- Contrato estático para evitar volver accidentalmente a imports eager de las pantallas pesadas.
