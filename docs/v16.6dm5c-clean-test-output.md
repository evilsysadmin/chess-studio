# v16.6dm5c · Clean expected-error test output

- `combatSession.test.js` silencia el `console.error` esperado al inyectar un snapshot corrupto.
- El test sigue verificando explícitamente que se registra el error y que la sesión se recupera desde memoria.
- No cambia el comportamiento de producción de `combatSession.js`; sólo evita ruido engañoso en Vitest.
