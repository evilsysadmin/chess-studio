# v16.6dg · Hotfix de contratos estáticos

No cambia reglas ni comportamiento del juego. Corrige tres expectativas obsoletas detectadas por la suite Vitest completa.

- `combatBattleLayout.test.js` deja de fallar por mencionar `useLayoutEffect` únicamente dentro de un comentario y comprueba que no exista una llamada real.
- El guard de batalla Combat valida el helper `canReturnCombatToSetup()` y su regla centralizada, en vez de exigir que la condición esté duplicada dentro del controller.
- `combatOperationalUx.test.js` valida el flujo actual de mejoras: ficha fijada y botones deshabilitados por coste individual de XP, en vez del antiguo `pinned && bankedXp > 0`.
- Los tres fallos eran tests rancios; el código funcional ya implementaba el comportamiento correcto.

Validación offline de esta iteración:

- `make static-preflight`: PASS.
- `node scripts/test_suite_audit.mjs --ci-wiring`: PASS.
- `python3 scripts/repo_doctor.py`: 12/12 PASS.
- Los dos contratos afectados pasan de forma aislada contra los ficheros reales que inspeccionan.

La suite Vitest completa sigue siendo la autoridad final en un entorno con `npm ci`.
