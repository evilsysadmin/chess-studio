# v16.6dm29c · Qwen non-thinking + validación de puzzles

- Qwen3 recibe `/no_think` en retratos y análisis ricos. Qwen3 piensa por defecto y los presupuestos muy cortos podían agotarse antes de producir contenido final; se amplía además el margen de salida sin ampliar lo que llega a UI.
- El parser del Worker acepta `message.content` como string o array de partes y registra `finish_reason` + presencia de reasoning cuando una respuesta queda vacía, sin registrar contenido.
- Los puzzles ya no comparan SAN como texto exacto. La jugada esperada se resuelve sobre el FEN actual y se compara por origen/destino/promoción, evitando falsos negativos por sufijos `+/#` o normalizaciones SAN.
- La horquilla curada `b5-c7+` queda cubierta explícitamente por test.
