# v16.6dm29e · Health gate robusto + feedback resuelto plegado

## Cloudflare Workers AI

El preflight estático ya no exige una representación byte-a-byte del diccionario JSON/Python embebido en el workflow. Acepta espacios/formato equivalentes, pero sigue exigiendo el modelo `analysis` esperado y que el health live compare todos los modelos del routing.

## Admin feedback

Los feedbacks `new` y `read` permanecen en el flujo principal. Los `resolved` se muestran bajo un `<details>` `Resueltos (N)` cerrado por defecto. El historial no se pierde y cada elemento puede reabrirse.
