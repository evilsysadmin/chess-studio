# v16.6dm3 · E2E reproducible + toolchains

- `e2e/package-lock.json` para Playwright 1.62.1.
- Local y CI instalan E2E con `npm ci`.
- `.python-version` fija Python 3.13 y `.nvmrc`/`.node-version` Node 22, alineados con CI/Docker.
- Playwright añade regresión real: un admin pulsa `X usuarios online` y aterriza en Panel Admin.
